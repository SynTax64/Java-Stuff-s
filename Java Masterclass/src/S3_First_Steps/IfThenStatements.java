package S3_First_Steps;

public class IfThenStatements {
    public static void main(String[] args) {
        boolean isAlien = false;

        if (isAlien == true) ; // it means, that this statemens will be executed in both examples
        // is Alien equals to false, or same example !isAlien
        System.out.println("It's not an alien");
    }
}
